import axios from "axios";
import { BASE_URL } from "../../config";
import { useRecoilCallback, useRecoilState, CallbackInterface  } from "recoil";
import { currentUser } from "../atoms/user";
import { useCallback } from "react";

export const fetchData = useRecoilCallback(({ snapshot, set}) 

}
